# Cookbook-UI

todo list

- Add way to delete recipe
- Change default pic to something sensible
- When opening the edit page, put the tags selected at the top? Not sure if this is desirable actually
- Move logic to fetch recipe and tags and combine them into hook
- If yield includes characters (instead of just numbers, then add "servings" otherwise leave as is like in the case of 1 loaf)
- Replace vanilla html with components
- Don't try to make a link if source isn't a link
- Validation on cook and prep time and yield inputs (number inputs)
- Testing adding tags on mobile
- Support for multiple pics
- Show preview of pic
- Automatic conversion from weight to volume and vic versa
- Cache results of fetch recipes
- Add color to tags
- Add a cron job to keep supabase alive
